

@model AppointmentBookingViewModel
@{
    ViewData["Title"] = "Create";
}

<div class="col offset-4 pt-5 ">
    <h1>Add details of Customer</h1>
    <form asp-controller="AppointmentBooking" asp-action="Create" method="post">
        <div class="row">
            <div class="col-md-7">
                <div class="mb-2 ">
                    <label asp-for="Date" class="form-label"></label>
                    <input type="date" asp-for="Date" class="form-control" />
                    <span asp-validation-for="Date"></span>
                </div>
                <div class="mb-2">
                    <label asp-for="Time" class="form-label"></label>
                    <input type="time" asp-for="Time" class="form-control" />
                    <span asp-validation-for="Time"></span>
                </div>
                <div class="mb-2">
                    <label asp-for="DocSpecializationId" class="form-label"></label>
                    <input type="text" asp-for="DocSpecializationId" class="form-control" />
                    <span asp-validation-for="DocSpecializationId"></span>
                </div>
                @*<div class="mb-2">
            <label asp-for="DocSpecializationId" class="form-label"></label>
            <select asp-for="DocSpecializationId" asp-items="@(new SelectList(Model.Specialization,"Id","SpecializationName"))" class="form-select">
                <option value="">---Select---</option>
            </select>
            <span asp-validation-for="DocSpecializationId"></span>
        </div>*@
                <div class="mt-2 d-grid">
                    <input type="submit" class="btn btn-primary" value="Add New" />
                </div>
            </div>
        </div>
    </form>
</div>
@section Scripts{
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial.cshtml");
    }
}